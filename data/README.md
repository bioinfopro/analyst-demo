# Input data placeholder

No sequencing data is included in this repository.

Expected locations:

- `fastq/`: paired-end FASTQ files named in `config/samplesheet.csv`
- `reference/microbial_reference.fasta`: microbial reference genome
- `reference/microbial_annotation.gtf`: gene annotation for RNA-seq counting

The folders remain empty until real or separately supplied assessment data is mounted here.